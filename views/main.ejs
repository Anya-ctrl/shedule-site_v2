<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Головна</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/reset.css">
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <%- include('./partials/header') %>

    <main class="main">
        <section class="main-page" id="main-page">
            <div class="container">
                
                <h2 class="main-page__title">
                    Головна сторінка
                </h2>
        
                <form class="main-page__selection" method="post">

                    <div class="button">
                        <% if (auth) { %>
                            <a id="scheduleLink" href="#">Студенти нашого ВУЗу</a>
                        <% } else { %> 
                            <a href="/registration">Студенти нашого ВУЗу</a>
                        <% } %>
                    </div>

                    <div class="button">
                        <% if (auth) { %>
                            <a id="scheduleLink" href="#">Викладачі нашого ВУЗу</a>
                        <% } else { %> 
                            <a href="/registration">Викладачі нашого ВУЗу</a>
                        <% } %>
                    </div>

                    <div class="button">
                        <% if (auth) { %>
                            <a id="scheduleLink" href="#">Розклад студентів</a>
                        <% } else { %> 
                            <a href="/registration">Розклад студентів</a>
                        <% } %>
                    </div>

                    <div class="button">
                        <% if (auth) { %>
                            <a id="scheduleLink" href="#">Розклад викладачів</a>
                        <% } else { %> 
                            <a href="/registration">Розклад викладачів</a>
                        <% } %>
                    </div>
        
                </form>

                <!-- <div class="pb"></div> -->
                
            </div>
        </section>
    </main>

    <script src="js/main.js"></script>
</body>
</html>